.pdf-review/
__pycache__/
*.pyc
.DS_Store

# Runtime state written into the skill directory by overleaf.py.
# session.json holds an Overleaf session cookie — never commit it.
projects.json
session.json
askpass.sh
