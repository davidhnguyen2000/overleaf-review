.pdf-review/
__pycache__/
*.pyc
.DS_Store

# Runtime state written into the skill directory by overleaf.py.
# credentials.json holds the Overleaf git token on machines with no system
# keyring, and session.json holds a session cookie. Never commit either.
projects.json
session.json
credentials.json
askpass.sh
